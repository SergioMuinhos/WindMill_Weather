<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        
        <item name="android:windowBackground">@color/weather_background</item>
        <item name="android:colorBackground">@color/weather_background</item>
        <item name="colorSurface">@color/weather_surface</item>
        <item name="colorSurfaceVariant">@color/weather_surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        
        <!-- styling for Material components -->
        <item name="materialCardViewStyle">@style/Widget.Material3.CardView.Elevated</item>
    </style>

    <!-- Tema para la pantalla de carga oficial (API 12+) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Color de fondo de la pantalla de carga (azul en claro, gris oscuro en noche) -->
        <item name="windowSplashScreenBackground">@color/colorPrimary</item>
        <!-- Icono central de la aplicación (utilizará el icono adaptable) -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- Tema que se aplica a la actividad tras cerrarse el splash screen -->
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

</resources>
